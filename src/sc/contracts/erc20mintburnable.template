// SPDX-License-Identifier: MIT
pragma solidity ^0.8.9;

import "./ERC20.sol";
import "./ERC20Burnable.sol";

contract ERC20MintBurnableTemplate is ERC20, ERC20Burnable {
    constructor() ERC20("{{name}}", "{{symbol}}", {{totalSupply}}) {}

    function mint(address to, uint256 amount) public onlyOwner {
        _mint(to, amount);
    }
}